# Agent Instructions

For any reading-note task, read `_rules/workflow-rules.md` first.

Follow its rule routing. Load other rule files only when the current step
needs them.

Use `_rules/question-guide.md` before writing notes.
