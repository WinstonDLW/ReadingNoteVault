# Agent Instructions

For any reading-note task, read `_rules/workflow-rules.md` first and
follow its routing.

Do not create temporary renders, screenshots, extracted pages, QA files,
or other intermediate artifacts inside this vault. Use an operating-system
temporary directory outside the workspace and remove the intermediates
when the task is complete. Write only requested final vault artifacts and
their rule-required source or log updates into this workspace.
